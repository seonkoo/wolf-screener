# 小狼选股雷达 · 策略逻辑审计与纠错机制

> 审计对象：`auto_screener.py`（四层筛选 + 好公司优先）+ `opt_backtest.py`（回测沙盒）
> 数据基准：`opt_result.json`（1524 个历史信号点，2023–2025 跨期）
> 生成日期：2026-08-01

---

## 一、逻辑合理性（是否讲得通）

### 1.1 四层结构本身——合理，且偏保守

| 层 | 逻辑 | 判定 | 评价 |
|---|---|---|---|
| L1 情绪海选 | 750 日价格分位"贪婪指数"，<40 = 恐慌低位 | pass/fail/neutral | ✅ 经典均值回归入口，避免追高 |
| L2 浪型过滤 | 日线 MACD 金叉/绿柱缩短 | pass/wait/fail | ✅ 过滤主跌段，降低"接落刀" |
| L3 技术共振 | 15min MACD 金叉/底背离 + 布林支撑，需 tech≥2 | pass/wait/fail | ✅ 多周期确认，减少假信号 |
| L4 资金校验 | 近 3 日主力净流入 | pass/wait/fail | ⚠️ 见 1.4 |

整体是**"低位 + 技术确认卖压衰竭 + 资金不再外流"三重保险**的抄底框架，结构自洽、风控前置，不追涨、不裸买。

### 1.2 本质：均值回归 / 恐慌低吸型，**强 regime 依赖**

这是理解整套策略的钥匙。回测里一个必须正视的结果：

> **全部 7 个变体的「超额收益」（策略 − 同期全市场等权持有）都为负**，最优 V3 也仅 **−2.6%**，基准（全市场无脑持有 40 日）均值约 **+8.5%**。

含义：
- 策略**不是 Alpha 增强型**，而是**风险控制型**。它系统性回避了处于强势（贪婪>40）的股票，只买被打压的票——在动量/牛市里，这等于主动放弃最强的那批票，因此跑输指数**是回测已证实的预期，不是 bug**。
- 它的真实价值在**震荡市**（均值回归最有效）和**急跌后的修复**（恐慌低位+共振）。在单边牛市里，正确做法是减仓本策略、加配指数。

**结论**：逻辑合理，但必须配合"市场状态开关"使用，否则牛市里会让人误以为策略失效。（纠错机制的 L1 层即解决此问题。）

### 1.3 好公司过滤不增 Alpha——已正确处理

回测：好公司前向胜率 64.7% / 均值 +5.4%，非好公司 65.5% / +5.9%。**两者前向收益几乎相同**，但硬 gate 会丢弃 93% 的信号。当前代码已改为"优先级/标注"而非硬门槛，这是正确的。

### 1.4 资金流（L4）是相对最弱的一环

- 沙箱环境主力净流入为**价量估算**（`estimated=True`），精度有限；网页端用东财真实数据更准。
- L4 仅作"校验"而非"否决"：即使资金面不明，只要 L1+L3 通过仍可进 A（非好公司轻仓）。设计上没让它一票否决，避免过度依赖弱信号，合理。

### 1.5 已识别的偏差 / 局限（诚实清单）

1. **幸存者偏差**：回测池仅含当前存续股票，不含已退市/暴雷股。真实胜率应略低于回测（差股在 40 日后可能已 ST/退市，回测看不到）。
2. **未来函数已防**：业绩报表用信号时点 `as-of` 已披露期次，ROE/现金流用 `financial_analysis_indicator` 按 as-of 切片。这一点做得对。
3. **过拟合风险评估**：贪婪阈值 35/40/45 三档胜率 65.5%/64.7%/64.3%，差异极小 → **阈值不脆弱，鲁棒**。但"持有 40 日 vs 10 日"差异巨大（+6% vs +1.7%），属真实发现而非过拟合。
4. **15min 代理偏差**：沙箱取不到 15min K 线，L3 用日线代理（布林/MA20/放量/日线底背离）。网页端用真实 15min 复核可得更严格结论，两者 L1/L2/L4 一致。
5. **无交易成本建模**：回测用收盘对收盘，未计佣金+印花税（约 0.12% 往返）。对 +5.9% 均值影响很小，但熊市高频止损时会侵蚀。

---

## 二、预期盈利率（能赚多少）

### 2.1 单笔期望收益（最优配置 V3：贪婪<35、持有40日、止损8%、止盈15%）

| 指标 | 值 |
|---|---|
| 样本数 | 1524 |
| **胜率** | **65.5%** |
| **单笔均值** | **+5.91%** |
| 单笔中位 | +6.03% |
| 触发止损 | 22.8%（≈ −8%） |
| 触发止盈 | 37.2%（≈ +15%） |
| 持有到期 | 40.0%（≈ +5.4%，反推） |

**单笔期望（毛）≈ +5.9%**；扣交易成本后约 **+5.8%**。也就是：严格按信号 + 8% 硬止损 + 15% 止盈，历史平均每笔约赚 6 个点，十笔里约 6.5 笔赚钱。

### 2.2 组合层预期（关键提醒）

- 每天可能同时出现 **20+ 个 A 信号**，但若等权全买，它们往往同处弱势板块→**高相关**，分散效果远不如"1524 个独立信号"的回测假设。
- 纠错机制默认 **最多 6 个同时持仓、单笔 ≤20% 仓位**，是为抑制这种相关性集中风险。
- **年化不可简单乘**：40 日持有 ≈ 每年 9 个不重叠周期，naive 乘成 "+50%/年" 是错的（信号重叠、相关性、regime 切换都会吃掉）。保守现实预期：在震荡市、纪律执行下，**年化两位数附近、最大回撤由 8% 硬止损+仓位上限共同封顶**。

### 2.3 诚实框架（务必记住）

> 这套策略的预期收益**跑不赢牛市里的指数**。它的意义是：
> 1. 用硬止损把单笔亏损锁死在 8%；
> 2. 在震荡/急跌修复里稳定吃均值回归的 6 个点；
> 3. 用市场状态开关避免在牛市里硬扛、在熊市里接刀。
>
> 把它当**"带止损的反弹捕手"**，而不是"战胜市场的圣杯"。

---

## 三、纠错机制（已落地：`strategy_guard.py`）

`strategy_guard.py` 每日随扫描运行，产出 `strategy_guard.json`，并在网页"自动选股"顶部显示体检横幅。三层：

### L0 逻辑自检
读 `opt_result.json`，核对参数是否仍在回测安全区；并固化那条"负超额警示"——牛市跑输指数时提示**这是预期内，不是策略失效**，防止误判。

### L1 市场状态（动态仓位 + 动态止损）
取沪深300日K线，按 `现价 vs MA200` + `MA60 斜率` + `MA60/MA200 死叉` 判四态：

| 状态 | 条件 | 仓位系数 | 止损 |
|---|---|---|---|
| 🟢 RANGE 震荡 | 其余 | 1.0x | 8% |
| 🟡 WEAK 弱势 | 价 < 年线 | 0.7x | 7% |
| 🟡 BULL 牛市 | 价>MA60>MA200 且 MA60 斜率>1% | 0.5x | 8% |
| 🔴 BEAR 熊市 | MA60 下穿 MA200（死叉） | 0.6x | 6% |

（2026-08-01 实跑：沪深300 收 4588 < MA200 4699 → **WEAK，仓位 0.7x、止损 7%、风险等级黄**。）

### L2 实盘回检（闭环证伪）
若提供 `trade_log.json`（用户记录真实买卖），滚动最近 20 笔算胜率/均值，与基线（65.5% / +5.9%）比对：
- 滚动胜率 <45% 或 均值 <0 → 🔴 RED，建议暂停新开仓；
- 滚动胜率 <55% 或 均值 <基线一半 → 🟡 AMBER，减半+收紧止损；
- 否则 🟢 GREEN。

**`trade_log.json` 格式**（每条一笔）：
```json
[{"code":"600036","name":"招商银行","entry_date":"2026-08-01",
  "entry":39.6,"exit_date":"2026-08-20","exit":41.3,"reason":"tp","pnl":0.0429}]
```
`reason` 可取 `tp`(止盈)/`stop`(止损)/`hold`(到期)/`manual`(手动)。

### 风险等级聚合
RED（参数漂移 / 实盘 RED）> AMBER（实盘偏弱 / 市场非震荡）> GREEN。网页横幅按等级变色并给出当日建议动作。

---

## 四、给使用者的实盘纪律建议

1. **先看体检横幅再动手**：黄/红时不按信号满仓。
2. **单笔必带 8% 硬止损**（熊市 6%），到点就走，不摊平。
3. **同时持仓 ≤6 只、单只 ≤20%**，避免弱势板块同跌。
4. **好公司（★）优先**，但非好公司轻仓也可做，二者历史收益无差。
5. **牛市请降低本策略权重**，把弹药给指数/趋势品种——这不是认输，是回测写明的。
6. **开始记 `trade_log.json`**，让 L2 实盘回检真正闭环，20 笔后就能知道你的实盘是否匹配回测预期。

---

*本报告基于历史回测，不构成投资建议。市场有风险，策略也可能在未见过的 regime 中失效——这正是 L1/L2 纠错层存在的意义。*
