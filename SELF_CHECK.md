# 自检报告与改进建议（2026-08-01）

> 基于实读代码（`auto_screener.py` / `national_team.py` / `sentiment.py` / `strategy_guard.py` / `patch_pages.py`）后的诚实排查。问题按严重程度分级，**是否优化由你决定**。

---

## ✅ 已确认正确（不用改）

| 检查项 | 结论 |
|---|---|
| 选股参数是否与回测一致 | `GREED_PASS=40`、`HOLD=40`、`STOP=8%`、`TP=15%` 全部对齐回测最优 ✓ |
| 是否按"主力净流入"预筛 | `get_universe` 改为按**跌幅**预筛，不按净流入（避免漏掉恐慌低位股）✓ |
| 好公司过滤是否为硬门槛 | 仅作"优先级标注"，不丢信号 ✓ |
| 所有 JSON 是否有 NaN 防护 | 五个模块均 `allow_nan=False` + `clean()` ✓ |
| 网页 Tab 按钮是否重复插入 | `patch_pages` 有 `data-tab not in s` 防重 ✓ |
| 快照烘焙是否幂等 | `sync_auto_tab` 标记间整段替换，连跑不翻倍 ✓ |

---

## 🔴 A 级：影响结论可信度，建议改

### A1. 国家队"进场托市"结论说得太满，且估算成交额分不清"涨价"还是"真买入"
**现象**：`national_team.py` 用 `估算成交额 = 成交量 × 100 × 均价` 的 5/20/60 日趋势判"上行/下行"，再结合"弱势市场"直接下结论 **「进场托市」**。
**为什么不合逻辑**：
- ETF 成交额放大，**可能只是价格涨了**（同样份额、单价更高 → 成交额更大），并不等于"有人净买入"。
- 弱势市场 + 成交额放大，**也可能是散户恐慌抛售的量**，不一定是国家队。
- 因此代码现在把"活跃度上升"直接等同于"大资金进场"，证据链有跳步。页面 `caveat` 虽注明是"推断"，但 `attitude` 字段写得很肯定。

**改进方案（三选一，由你定）**：
1. **软化措辞 + 加置信度**：结论改成"资金活跃度上升，疑似托市（中低置信）"，并把判定依据（趋势数值）摊开给用户自己判断。改动最小。
2. **加 ETF 折溢价**：若 ETF 市价相对基金净值出现**溢价**，说明需求 > 供给 = 真实买盘强。需另取净值接口（云端可达），中等工作量。
3. **加份额趋势**：用 akshare 取历史份额做"份额变化方向"，比成交额更贴近"申购/赎回"。但日频历史在沙箱取不到，需云端跑、工作量较大。

**工作量**：方案1 约 30 分钟；方案2/3 约半天。

### A2. 自动选股卡片的止损(8%) 与 策略体检的动态止损(7%) 自相矛盾
**现象**：`auto_screener.py` 卡片写死"止损 8%"，但 `strategy_guard.py` 在弱势市建议"止损 7%"。同一页面同时出现两个不同止损数，用户会困惑"到底听谁的"。
**为什么不合逻辑**：策略体检明明算出了"当前该 7%"，却没传回选股卡片，等于两套系统各说各话。
**改进方案**：让 `auto_screener` 在生成卡片时**读取 strategy_guard 的 regime→stop 映射**，弱势用 7%、其余用 8%。或至少在体检横幅注明"此为建议参考值，卡片以策略内置为准"。
**工作量**：约 1 小时（改 auto_screener 读 guard 输出或内联同一张映射表）。

---

## 🟡 B 级：体验 / 可解释性，建议加

### B1. 五个维度没有"综合研判"总览——信号可能互相打架
**现象**：自动选股 / 蓝筹 / 事件 / 国家队 / 情绪 五个 Tab 各自独立，只有体检横幅附带一点仓位建议。用户要自己心算"国家队托市 + 情绪冰点 + 体检 0.7x"该怎么定。
**为什么不合逻辑**：当"体检说谨慎(0.7x)"与"情绪说冰点买"同时出现时，没有权重指引，新手容易不知所措。
**改进方案**：加一个 **「📊 总览」Tab 或首页横幅**，把五维合成一句话 verdict，例如：
`市场弱势 · 国家队疑似托市 · 情绪冰点(逆向偏买) · 体检建议 0.7x 仓 → 综合：可小仓分批低吸，严设 7% 止损`。
**工作量**：约半天（前端聚合 + 简单加权规则）。

### B2. 重大事件关键词引擎不会处理"否定词"
**现象**：`patch_pages` 里用关键词判利好/利空。例如含"关税"→利空，但"关税缓和/关税下调"也会命中"关税"被判利空；"美联储维持利率不变"不含"降息/加息"→归"关注"。
**为什么不合逻辑**：简单包含匹配，不懂语义否定，会偶有反向误判。
**改进方案**：加极小否定词表（"缓和/下调/收回/否认"前置时翻转结论），或换用轻量情感模型。
**工作量**：约 1~2 小时。

---

## 🟢 C 级：健壮性，可选

### C1. 联网失败且无离线快照时，面板永久转圈
**现象**：`loadJSON` 捕获失败后，若挂载点只有"正在加载"占位、没有烘焙快照（如首次部署、或某个 JSON 生成失败），用户会看到转圈一直转。
**改进方案**：fetch 失败时，若挂载点无实质内容，显示"⚠️ 数据加载失败，请稍后刷新"错误框 + 重试按钮。
**工作量**：约 30 分钟。

### C2. `patch_pages.py` 会重置离线快照，必须紧跟 `sync_auto_tab.py`
**现象**：重跑 `patch_pages` 会用含"正在加载"的模板**重建**面板，把之前烘焙进去的快照清空；只有紧接着跑 `sync_auto_tab` 才会重新填回。工作流里顺序是对的，但手动操作时容易踩。
**改进方案**：把"打补丁"和"烘焙"合并成一个命令，或在 patch 时直接保留原快照。
**工作量**：约 1 小时。

### C3. 回测幸存者偏差（已知残留风险）
**现象**：回测只用了"现在还在上市"的股票，已退市的没算进去 → 真实胜率可能略低于回测的 65.5%。
**说明**：这是量化回测的通病，不是代码 bug。已在 `STRATEGY_AUDIT.md` 如实标注。若要更严谨，需引入退市股票池，工作量很大，优先级低。

---

## 📋 我的优先级建议（仅供参考，你定）

| 优先级 | 项 | 理由 |
|---|---|---|
| 高 | **A2 止损一致** | 同一页面两个数字，最容易被你发现、也最该修 |
| 高 | **A1 软化国家队措辞** | 结论比数据支撑更满，涉及"信不信得过" |
| 中 | **B1 综合总览** | 直接提升可用性，把五个信号变成一句话 |
| 中 | **B2 事件否定词** | 偶有误判，修起来便宜 |
| 低 | C1/C2/C3 | 健壮性，不急 |

> 你可以只挑其中一两个，也可以全做，或者都不做。告诉我你的选择即可。
